/*
- 다중 정의 Overloading
  동일한 이름에 대하여 여러가지 의미를 부여하는 것

- 함수 다중 정의
  - 동일한 이름을 갖는 함수를 여러개 정의하는 것
    C 에서는 불가, C++ 에서만 가능하다.
  - 동일한 개념의 처리를 여러가지 데이터나 객체에 대해 각각의 대상에 맞는 처리를 해야할 경우 사용한다.
    Eg. C 언어일 때: fInt(), fDoule() 
        C++ 언어일 떄: f(int a), f(Double b)
  - 다중 정의된 함수의 구분: 인수(매개변수)의 개수 및 인수(매개변수)의 자료형. 
                             함수의 반환 자료형으로는 함수를 구분할 수 없다.
*/